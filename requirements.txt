# Voice2MIDI dependencies
#
# scipy is pinned to <1.12 because basic-pitch calls scipy.signal.gaussian,
# which was moved to scipy.signal.windows.gaussian in scipy 1.12 and removed
# from the top-level namespace. The monkey-patch in voice2midi.py handles
# environments where a newer scipy is unavoidable, but pinning here keeps
# fresh installs clean.
setuptools<72
basic-pitch
scipy<1.12
onnxruntime
rumps
